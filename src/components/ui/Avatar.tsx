import { cn } from "@/lib/utils";
export function Avatar({ name, src, className }: { name: string; src?: string | null; className?: string }) { return src ? <img src={src} alt={name} className={cn("h-10 w-10 rounded-full object-cover", className)} /> : <span className={cn("flex h-10 w-10 items-center justify-center rounded-full bg-background text-sm font-semibold text-white", className)}>{name.split(" ").map((part) => part[0]).slice(0, 2).join("")}</span>; }
