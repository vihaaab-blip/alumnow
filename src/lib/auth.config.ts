import type { NextAuthConfig } from "next-auth";
export const authConfig = { pages: { signIn: "/login" }, callbacks: { authorized({ auth, request: { nextUrl } }) { const loggedIn = Boolean(auth?.user); const protectedPath = ["/browse","/bookings","/dashboard","/profile","/alumni","/apply","/account"].some((p) => nextUrl.pathname.startsWith(p)); if (nextUrl.pathname.startsWith("/admin")) return auth?.user?.role === "admin"; return !protectedPath || loggedIn; } }, providers: [] } satisfies NextAuthConfig;
